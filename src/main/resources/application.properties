spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:mysql://localhost:3306/sgpi}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:sgpi}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:sgpi123}
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.open-in-view=false

jwt.secret=${JWT_SECRET:sgpi-unpa-secret-key-que-debe-cambiarse-en-produccion-2026}
jwt.expiration-ms=${JWT_EXPIRATION_MS:3600000}
